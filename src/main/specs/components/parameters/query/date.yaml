name: date
in: query
required: false
description: >-
  > Represents the effective date. Will use current date by default.
  
  <!-- -->
  
  > Date format: YYYY-MM-DD
example: '2023-01-29'
schema:
  type: string
  format: date
