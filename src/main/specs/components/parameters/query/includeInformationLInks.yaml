name: include_information_flag
in: query
required: true
description: >-
  > A flag to indicate whether the information links are to be included or not. True if information links are to be included.
schema:
  type: boolean
  default: false
  nullable: true
